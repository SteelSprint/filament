Collapse runs iteratively. In each pass, any marker whose every edge is checked gets its baseline hash updated to the current scan hash and its resolution entries pruned. Same for specs. When a node's scan hash is empty string (deleted), the node is removed from the result and its links are filtered out. Passes repeat until no node collapses. This enables progressive collapse across many-to-many topologies.