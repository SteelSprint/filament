FileScanner follows imports starting from main.drift.xml, parses each .drift.xml as XML, extracts spec elements with their id attribute, SHA1-hashes the trimmed inner content, and returns Spec records with module-qualified ID, Hash, Filepath, and LineNumber.