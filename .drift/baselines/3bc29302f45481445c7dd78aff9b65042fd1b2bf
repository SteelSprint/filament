The init command creates .driftpin/ (containing state.xml and baselines/), and if main.pin.xml does not already exist, writes a starter template with a single example spec. The output message instructs the user to edit main.pin.xml, place markers, and run drift skill.