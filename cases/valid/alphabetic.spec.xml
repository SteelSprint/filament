<?xml version="1.0" encoding="UTF-8"?>
<spec name="alphabetic">

  <definitions>
    <term text="filament">The library under test.</term>
  </definitions>

  <section id="public_api" label="Public API">
    <clause id="public_api.function">The library exposes assert_spec_hash.</clause>
    <clause id="public_api.signature">The signature takes a test handle, a path, and a hash.</clause>
  </section>

  <section id="hash_format" label="Hash format">
    <clause id="hash_format.encoding">A hash is a lowercase hex string of 64 characters.</clause>
    <clause id="hash_format.algorithm">The algorithm is SHA-256, applied to <ref>public_api.function</ref>'s content transitively.</clause>
  </section>

</spec>
