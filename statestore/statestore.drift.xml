<module name="pinstore">
<spec id="load">FileStateStore.Load reads .drift/state.xml from disk, unmarshals XML into Specs, Markers, Links, and ResolutionState, and returns the State. If the file does not exist, it returns ErrStateNotFound.</spec>
<spec id="save">FileStateStore.Save marshals State to indented XML and writes it to .drift/state.xml, truncating any existing file. It also ensures .drift/baselines/ exists. The XML root element is drift with nested specs, markers, links, and resolutions sections.</spec>
<spec id="not_found">When .drift/state.xml does not exist on disk, Load returns ErrStateNotFound, prompting the user to run drift init first.</spec>
<spec id="baselines">BaselineStore manages content-addressed baseline files in .drift/baselines/. Each file is named by its SHA1 hash (sha1(content)==filename). Write stores content at &lt;hash&gt; if absent (dedup) and verifies sha1(content)==hash. Read returns content for &lt;hash&gt; or false if missing. Delete removes a single file. Missing files are not errors.</spec>
</module>
