<main>
<spec id="validate">ValidateUser rejects users with empty names by returning an error. No other validation is performed — names of any non-empty length are accepted.</spec>
<spec id="format">FormatUser returns a display string in the format "User: " followed by the name. No other formatting is applied.</spec>
<spec id="perms">CheckPermission returns true only for the role "admin". No other roles are granted permission under any circumstances.</spec>
</main>
